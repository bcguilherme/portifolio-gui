/**
 * Minimalist Portfolio
 * Most interactions are handled via CSS (smooth scrolling, hover effects).
 */

document.addEventListener('DOMContentLoaded', () => {
  console.log('Portfolio loaded');
});